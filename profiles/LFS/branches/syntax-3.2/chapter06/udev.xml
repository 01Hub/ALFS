<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE alfs SYSTEM "../DTD/ALFS.dtd" [
<!ENTITY % general_entities SYSTEM "../config/general.ent">
<!ENTITY % package_entities SYSTEM "../config/package.ent">

%general_entities;
%package_entities;
]>

<alfs>

<package name="udev" version="&udev-version;">
	<stage name="Unpacking">
		<unpack base="&build_dir;">
			<digest>&udev-md5;</digest>
			<archive>&packages_dir;/&udev-package;</archive>
		</unpack>
	</stage>

	<stage name="Installing">
		<stageinfo>
			<base>&build_dir;/&udev-directory;</base>
		</stageinfo>

		<make>
		  	<param>udevdir=/dev</param>
		</make>

		<make>
		  	<param>udevdir=/dev</param>
			<param>install</param>
		</make>

		<copy base="&packages_dir;">
        		<option>force</option>
        		<source>&udev-permissions-file;</source>
        		<destination>/etc/udev/permissions.d/00-lfs.permissions</destination>
		</copy>

		<copy base="&packages_dir;">
        		<option>force</option>
        		<source>&udev-rules-file;</source>
        		<destination>/etc/udev/rules.d/00-lfs.rules</destination>
		</copy>		
	</stage>

	<stage name="Cleanup">
		<remove>
			<file>&build_dir;/&udev-directory;</file>
		</remove>
	</stage>
</package>

</alfs>
