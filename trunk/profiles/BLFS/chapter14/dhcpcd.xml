<package name="dhcpcd" version="&dhcpcd-version;">

	<stage name="Unpacking a package.">
		<unpack>
			<archive>&packages_dir;/&dhcpcd-package;</archive>
			<destination>&build_dir;</destination>
		</unpack>
	
		<unpack>
			<archive>&packages_dir;/&blfs-bootscripts-package;</archive>
			<destination>&build_dir;</destination>
		</unpack>

		<copy>
			<source>&packages_dir;/&dhcpcd-patch;</source>
			<destination>&build_dir;</destination>
		</copy>
	</stage>

	<stage name="Installing a package.">
		<stageinfo>
			<base>&build_dir;/&dhcpcd-directory;</base>
		</stageinfo>

		<patch>
			<param>-N</param>
			<param>-p1</param>
			<param>-i ../&dhcpcd-patch;</param>
		</patch>

		<configure>
			<param>--prefix=""</param>
			<param>--sysconfdir=/var/lib</param>
			<param>--mandir=/usr/share/man</param>
		</configure>

		<make />

		<make>
			<param>install</param>
		</make>
	</stage>

	<stage name="Install dhcpcd service script">
		<stageinfo>
			<base>&build_dir;/&blfs-bootscripts-directory;</base>
		</stageinfo>

		<make>
			<param>install-service-dhcpcd</param>
		</make>
	</stage>

	<stage name="Configure dhcpcd">
		<textdump>
			<file>/etc/sysconfig/network-devices/ifconfig.eth0</file>
			<content>
			=ONBOOT=yes
			=DHCP_START="&dhcpcd-start-params;"
			=DHCP_STOP="-k &dhcpcd-stop-params;"
			=SERVICE=dhcpcd
			</content>
		</textdump>
	</stage>

	<stage name="Clean-up.">
		<remove>&build_dir;/&dhcpcd-directory;</remove>
		<remove>&build_dir;/&dhcpcd-patch;</remove>
		<remove>&build_dir;/&blfs-bootscripts-directory;</remove>
	</stage>

</package>
