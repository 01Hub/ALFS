<package name="freetype" version="&freetype-version;">

	<stage name="Unpacking a package.">
		<unpack>
			<archive>&packages_dir;/&freetype-package;</archive>
			<destination>&build_dir;</destination>
		</unpack>
		<copy>
		        <source>&packages_dir;/&freetype-patch;</source>
			<destination>&build_dir;</destination>
		</copy>
	</stage>

	<stage name="Installing a package.">
		<stageinfo>
			<base>&build_dir;/&freetype-directory;</base>
		</stageinfo>

		<patch>
			<param>-N</param>
			<param>-p1</param>
			<param>-i ../&freetype-patch;</param>
		</patch>

		<configure>
			<param>--prefix=/usr</param>
		</configure>

		<make/>

		<make>
			<param>install</param>
		</make>

		<execute command="/sbin/ldconfig" />

	</stage>

	<stage name="Clean-up.">
		<remove>&build_dir;/&freetype-directory;</remove>
		<remove>&build_dir;/&freetype-patch;</remove>
	</stage>

</package>
