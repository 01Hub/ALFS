<package name="kdoc" version="&kdoc-version;">

        <packageinfo>
                <requires><name>kdebase</name></requires>
        </packageinfo>

	<stage name="Unpacking a package.">
		<unpack>
			<archive>&packages_dir;/&kdoc-package;</archive>
			<destination>&build_dir;</destination>
		</unpack>
	</stage>

	<stage name="Installing a package.">
		<stageinfo>
			<base>&build_dir;/&kdoc-directory;</base>
		</stageinfo>

		<configure>
                        <param>--prefix=/opt/kde-&kde-version;</param>
		</configure>

		<make />

		<make>
			<param>install</param>
		</make>

	</stage>

	<stage name="Clean-up.">
		<remove>&build_dir;/&kdoc-directory;</remove>
	</stage>

</package>
