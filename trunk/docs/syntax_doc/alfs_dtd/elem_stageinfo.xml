<sect1 id="elem_stageinfo" xreflabel="Element: &lt;stageinfo&gt;">
<?dbhtml filename="elem_stageinfo.html" dir="alfs_dtd"?>
<title>Element: &lt;stageinfo&gt;</title>

<sect2>
<title>Syntax</title>

<synopsis>
&lt;!ELEMENT stageinfo      (root?, user?, environment?, base?)&gt;
</synopsis>

<para>This element occurs in the element&nbsp;:&nbsp;&nbsp;
<xref linkend="elem_stage"/>
</para>

<para>See also&nbsp;:&nbsp;&nbsp;
<xref linkend="elem_root"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_base"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_environment"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_user"/>
</para>

</sect2>

<sect2>
<title>Description</title>

<para>The element <filename>stageinfo</filename> allows to specify several
context informations used to process the commands of the stage to which the
stageinfo belongs.</para>

<para>The <filename>base</filename> sub-element specify a base directory
that will be used by all the commands of the stage, unless that command
redefines the base locally.</para>

<para>The <filename>user</filename> sub-element specify a user name that
will be used as the effective user ID of the current process for all the
commands of the stage</para>

<para>The <filename>root</filename> sub-element specify a directory name in
which the commands of the stage will be performed (using a chroot
command).</para>

<para>The <filename>environment</filename> sub-element allow to specify a
environment variable that will be added to the current environment when
processing the commands of the stage.</para>

</sect2>

<sect2>
<title>Examples</title>

<screen><userinput>&lt;stage&gt;
	&lt;stageinfo&gt;
		&lt;base&gt;/usr/src/gzip.1.2.4a&lt;/base&gt;
	&lt;/stageinfo&gt;

	&lt;configure /&gt;

	&lt;make /&gt;

	&lt;make&gt;
		&lt;param&gt;install&lt;/param&gt;
	&lt;/make&gt;
&lt;/stage&gt;</userinput></screen>

<para>The equivalent bash script is&nbsp;:</para>

<screen><userinput>echo Executing configure
cd /usr/src/gzip.1.2.4a
<command>./configure</command>

echo Executing make
cd /usr/src/gzip.1.2.4a
<command>make</command>

echo Executing make
cd /usr/src/gzip.1.2.4a
<command>make install</command>

echo Exiting stage</userinput></screen>

</sect2>

</sect1>
