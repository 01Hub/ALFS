<sect1 id="elem_textdump" xreflabel="Element: &lt;textdump&gt;">
<?dbhtml filename="elem_textdump.html" dir="alfs_dtd"?>
<title>Element: &lt;textdump&gt;</title>

<sect2>
<title>Syntax</title>

<synopsis>
&lt;!ELEMENT textdump       (file, content)&gt;
&lt;!ATTLIST textdump
             base           CDATA #IMPLIED
             mode           CDATA #IMPLIED&gt;
</synopsis>

<para>This element occurs in the elements&nbsp;:&nbsp;&nbsp;
<xref linkend="elem_alfs"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_stage"/>
</para>

<para>See also&nbsp;:&nbsp;&nbsp;
<xref linkend="elem_file"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_content"/>
</para>

</sect2>

<sect2>
<title>Description</title>

<para>The element <filename>textdump</filename> is one of the many main
operation elements.  The sub-element <filename>file</filename> contains the
name of the file to be created.</para>

<para>The sub-element <filename>content</filename> is used to specify the
content of the file being created when processing a
<filename>textdump</filename> operation. When processing the textual
content of this element, one removes the leading blanks up to and including
a '=' character.</para>

<para>The attribute <filename>base</filename> allows to specify the
directory in which the file creation will be perforemd.</para>

<para>The attribute <filename>mode</filename> allows to specify the append
creation mode.</para>

</sect2>

<sect2>
<title>Examples</title>

<userinput><screen>&lt;textdump base=&quot;/etc&quot;&gt;
	&lt;file&gt;group&lt;/file&gt;

	&lt;content&gt;
		=root:x:0:
		=bin:x:1:
		=sys:x:2:
		=kmem:x:3:
		=tty:x:4:
		=tape:x:5:
		=daemon:x:6:
		=floppy:x:7:
		=disk:x:8:
		=lp:x:9:
		=dialout:x:10:
		=audio:x:11:
	&lt;/content&gt;
&lt;/textdump&gt;</screen></userinput>

<para>The equivalent bash script is&nbsp;:</para>

<userinput><screen>cd /etc
cat > group &lt;&lt; 'EOF'
root:x:0:
bin:x:1:
sys:x:2:
kmem:x:3:
tty:x:4:
tape:x:5:
daemon:x:6:
floppy:x:7:
disk:x:8:
lp:x:9:
dialout:x:10:
audio:x:11:
EOF</screen></userinput>

<userinput><screen>&lt;textdump mode=&quot;append&quot;&gt;
        &lt;file&gt;/etc/sysconfig/network&lt;/file&gt;
        &lt;content&gt;
                =GATEWAY=192.168.123.254
                =GATEWAY_IF=eth0
        &lt;/content&gt;
&lt;/textdump&gt;</screen></userinput>

<para>The equivalent bash script is&nbsp;:</para>

<userinput><screen>echo Generating file /etc/sysconfig/network
cat >> /etc/sysconfig/network &lt;&lt; 'EOF'
GATEWAY=192.168.123.254
GATEWAY_IF=eth0
EOF</screen></userinput>

</sect2>

</sect1>
