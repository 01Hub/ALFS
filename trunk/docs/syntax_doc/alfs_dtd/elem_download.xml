<sect1 id="elem_download" xreflabel="Element: &lt;download&gt;">
<?dbhtml filename="elem_download.html" dir="alfs_dtd"?>
<title>Element: &lt;download&gt;</title>

<sect2>
<title>Syntax</title>

<synopsis>
&lt;!ELEMENT download       (file, url*, destination, digest?)&gt;
</synopsis>

<para>This element occurs in the elements&nbsp;:&nbsp;&nbsp;
<xref linkend="elem_alfs"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_stage"/>
</para>

<para>See also&nbsp;:&nbsp;&nbsp;
<xref linkend="elem_file"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_url"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_destination"/>&nbsp;&nbsp;|&nbsp;&nbsp;
<xref linkend="elem_digest"/>
</para>

</sect2>

<sect2>
<title>Description</title>

<para>The element <filename>download</filename> is one of the many main
operation elements. It is used to specify one or several URLs from which a
file can be downloaded, and optionally a digest to check that the file
being downloaded if the right one.</para>

<para>The sub-element <filename>file</filename> contains the name of the
file to be downloaded.</para>

<para>The sub-element <filename>url</filename> contains the url of the
directory from which the download is performed. It should be terminated by
a slash [&nbsp;/&nbsp;]. More precisely, the actual URL used for the
download is the catenation of the content of the <filename>url</filename>
element with the content of the <filename>file</filename> element. This
element is optional, but if used can be used more than once.</para>

<para>The sub-element <filename>destination</filename> contains the path of
the directory into which the download should be performed.</para>

<para>The sub-element <filename>digest</filename> contains the value with
which the digest computed from the dowloaded file (e.g. computed using the
md5 algorithm) will be compared. This sub-element is optional.</para>

<para>If the file is already present in the destination directory, no
download is performed but the digest is checked if present.</para>

</sect2>

<sect2>
<title>Examples</title>

<screen><userinput>&lt;download&gt;
	&lt;file&gt;kbd-1.08.tar.gz&lt;/file&gt;
	&lt;url&gt;ftp://ftp.win.tue.nl/pub/linux-local/utils/kbd/&lt;/url&gt;
	&lt;destination&gt;/tmp/&lt;/destination&gt;
	&lt;digest&gt;924e6a05e52715dc0f96385b35f2f680&lt;/digest&gt;
&lt;/download&gt;</userinput></screen>

<para>There is no equivalent Bash script example for this element.</para>

</sect2>

</sect1>
